apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarqube-db-config
  namespace: build
spec:
  refreshInterval: "300s"
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: sonarqube-db-config
    creationPolicy: Owner

  data:
    - secretKey: jdbcOverwrite.jdbcUrl
      remoteRef:
        key: homelab/data/sonarqube
        property: jdbcUrl

    - secretKey: jdbcOverwrite.jdbcUsername
      remoteRef:
        key: homelab/data/sonarqube
        property: jdbcUsername

    - secretKey: jdbcOverwrite.jdbcPassword
      remoteRef:
        key: homelab/data/sonarqube
        property: jdbcPassword
